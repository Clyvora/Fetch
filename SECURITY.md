# Security policy

Report vulnerabilities privately to **security@clyvora.tech**. Do not include private URLs, tokens, or copyrighted audio in a report.

Fetch validates URLs both in the browser and in ClyvoraAPI. Browser checks are usability controls, not the backend security boundary. The backend must retain its strict host, redirect, request-size, response-size, timeout, and rate-limit policy.

